{
  "scripts": {
    "dev": "npm --prefix server run dev",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client-next run dev"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
